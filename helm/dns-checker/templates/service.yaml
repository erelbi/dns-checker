apiVersion: v1
kind: Service
metadata:
  name: {{ include "dns-checker.fullname" . }}
  labels:
    {{- include "dns-checker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dns-checker.selectorLabels" . | nindent 4 }}
